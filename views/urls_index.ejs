<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
    <% include ../views/partials/_header %>

</head>
<body>
  <main>
        <div class="jumbotron">
            <h1>This is great</h1>
            <p>Shortening Da URLS</p>
        <ul>
            <% for(let shortUrl in urlDatabase) { %>
              <p><%= shortUrl%> : <%= urlDatabase[shortUrl]%> </p>
              <form method="get" action="<%= urlDatabase[shortUrl] %>"> <input type="submit" value="Go to" /> </form>
              <form method="get" action="/urls/<%= shortUrl %>"> <input type="submit" value="edit" /> </form>
              <form method="POST" action="/urls/<%= shortUrl %>/delete"> <input type="submit" value= "delete" /> </form>
            <% }; %>

        </ul>

    <!-- Add your EJS code here -->
    <!-- list urls AND short forms -->
<!-- link for shortening URL -->

      </div>
  </main>
</body>
</html>


